#!/usr/bin/env node
const fs=require("fs"),path=require("path"),assert=require("assert");
const root=path.resolve(__dirname,"../.."),read=r=>fs.readFileSync(path.join(root,r),"utf8");
const m=read("backend/models/Notification.js"),c=read("backend/controllers/notificationController.js"),s=read("backend/sockets/notificationSocket.js"),t=read("src/components/dashboard/DashboardTopbar.jsx"),calls=read("backend/sockets/messageSocket.js");
assert(!m.includes('notificationSchema.post("save"'),"read must not use generic save-to-new lifecycle"); assert(!m.includes('notificationSchema.post("findOneAndUpdate"'),"update must not use creation fanout"); assert(m.includes("fanoutCreatedNotification")&&m.includes("emitNotificationUpdated"),"create/update lifecycle split required"); assert(c.includes("Notification.emitNotificationUpdated(notification)"),"HTTP read update missing"); assert(c.includes("exports.markAllRead") && c.includes("Notification.updateMany"),"mark-all-read must update existing notifications rather than create new ones"); assert(c.includes("Notification.emitNotificationUpdated(notification)"),"mark-all-read must broadcast each updated notification state"); assert(s.includes("Notification.emitNotificationUpdated(notification)"),"socket read update missing"); assert(!t.includes('socket.on("new-call-notification", onNotification);'),"call UI event must not increment general badge"); assert(calls.includes("suppressPush: true"),"call notification must suppress model push when call flow sends its own"); console.log("NOTIFICATION LIFECYCLE REGRESSION TEST PASSED");
