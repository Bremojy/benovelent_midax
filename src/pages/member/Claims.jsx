import { useEffect, useState } from "react";
import DashboardLayout from "../../layouts/DashboardLayout";
import API from "../../services/api";
import "../../styles/portalModule.css";

const STAGES=["Pending","Under Review","Documents Required","Eligibility Review","Approval Review","Approved","Disbursement Pending","Paid","Completed","Rejected","Cancelled","Closed"];
const stageCopy={Pending:"Submitted and awaiting assignment.","Under Review":"An administrator is checking the application and documents.","Documents Required":"More or clearer supporting documents are needed.","Eligibility Review":"Eligibility, membership and scheme rules are being checked.","Approval Review":"The case is being considered for final approval.",Approved:"Approved. The next step is processing/disbursement.","Disbursement Pending":"Approval is complete; funds are being prepared.",Paid:"Payment/disbursement has been recorded.",Completed:"The support process is complete.",Rejected:"The request was not approved. See the latest review reason below.",Cancelled:"This application has been cancelled.",Closed:"This application is closed."};
const money=v=>new Intl.NumberFormat("en-KE",{style:"currency",currency:"KES",maximumFractionDigits:0}).format(Number(v||0));
const fmt=v=>v?new Date(v).toLocaleString("en-KE",{day:"2-digit",month:"short",year:"numeric",hour:"2-digit",minute:"2-digit"}):"—";
export default function Claims(){const [claims,setClaims]=useState([]),[loading,setLoading]=useState(true),[error,setError]=useState("");const load=async()=>{try{setLoading(true);setError("");const {data}=await API.get("/member/claims");setClaims(Array.isArray(data?.claims)?data.claims:[]);}catch(e){setError(e.response?.data?.message||e.message||"Unable to load your claims.");}finally{setLoading(false);}};useEffect(()=>{load();},[]);return <DashboardLayout><div className="portal-module"><header className="portal-module-header"><div><span>MEMBER CLAIMS</span><h1>Claims & Applications</h1><p>Track exactly where every support request is in the review process, including notes and payment stages.</p></div><button className="portal-btn" onClick={load} disabled={loading}>{loading?"Refreshing…":"Refresh"}</button></header>{error&&<div className="portal-alert">{error}</div>}{loading?<div className="portal-empty">Loading your claims…</div>:claims.length===0?<div className="portal-empty"><h3>No claims yet</h3><p>Submit a support request from Support and it will appear here.</p></div>:<div className="portal-grid two">{claims.map(c=>{const status=c.status||"Pending";const idx=STAGES.indexOf(status);const history=Array.isArray(c.timeline)?c.timeline:[];return <article className="portal-panel" key={`${c.supportType}-${c._id}`}><div style={{display:"flex",justifyContent:"space-between",gap:10,alignItems:"start"}}><div><span className="portal-badge">{String(c.supportType||"support").toUpperCase()}</span><h2>{c.description||c.purpose||c.hospitalName||c.deceasedName||"Support application"}</h2><p>Submitted {fmt(c.createdAt||c.applicationDate)}</p></div><span className={`portal-badge ${["Rejected","Cancelled","Closed"].includes(status) ? "rejected" : ["Approved","Paid","Completed"].includes(status) ? "approved" : ""}`}>{status}</span></div><div className="portal-stat-grid"><div className="portal-stat"><span>Requested</span><strong>{money(c.requestedAmount||c.amount)}</strong></div><div className="portal-stat"><span>Approved</span><strong>{money(c.approvedAmount)}</strong></div>{c.repaymentEnabled&&<div className="portal-stat"><span>Repayment balance</span><strong>{money(c.balance)}</strong></div>}</div>{idx>=0&&<div style={{marginTop:12}}><strong>Review progress</strong><div style={{display:"grid",gap:8,marginTop:8}}>{STAGES.map((s,i)=><div key={s} style={{display:"grid",gridTemplateColumns:"18px 1fr",gap:8,opacity:i<=idx||status===s?1:.42}}><div style={{width:14,height:14,borderRadius:"50%",background:i<=idx?"#0f766e":"#cbd5e1",marginTop:3}}/><div><strong>{s}</strong><div style={{fontSize:13}}>{stageCopy[s]}</div></div></div>)}</div></div>}{status==="Rejected"&&<div className="portal-alert" style={{marginTop:12}}><strong>Reason:</strong> {c.rejectionReason||c.remarks||"Your application was declined."}</div>}{history.length>0&&<div style={{marginTop:14}}><h3>Review history</h3>{history.slice().reverse().map((h,i)=><div key={`${h.date}-${i}`} style={{padding:"10px 0",borderBottom:"1px solid #e5e7eb"}}><strong>{h.status}</strong><div>{h.remarks||"No additional note."}</div><small>{fmt(h.date)}</small></div>)}</div>}</article>})}</div>}</div></DashboardLayout>}
