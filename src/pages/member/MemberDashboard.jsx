import {useEffect,useState} from "react";import {Link} from "react-router-dom";import DashboardLayout from "../../layouts/DashboardLayout";import {getMemberDashboard} from "../../services/memberService";import API from "../../services/api";import "./MemberDashboard.css";export default function MemberDashboard(){const[d,setD]=useState(null),[c,setC]=useState(null),[e,setE]=useState(""),[loading,setLoading]=useState(true);const load=async()=>{try{const[a,b]=await Promise.all([getMemberDashboard(),API.get("/member/community-stats")]);setD(a?.dashboard||{});setC(b.data?.stats||{})}catch(x){setE(x.response?.data?.message||x.message||"Unable to load dashboard")}finally{setLoading(false)}};useEffect(()=>{load()},[]);if(loading)return <DashboardLayout><div className="portal-empty">Loading your dashboard...</div></DashboardLayout>;if(e)return <DashboardLayout><div className="portal-alert">{e}</div></DashboardLayout>;const member=d?.member||{},profile=d?.profileCompletion||{},pct=profile.percentage||member.profileCompletion||0;return <DashboardLayout><div className="member-dashboard"><section className="member-welcome"><div><span className="welcome-label">MEMBER PORTAL</span><h1>Welcome back, {member.fullName?.split(" ")[0]||"Member"} 👋</h1><p>See what is happening in the Benevolent Constitution and reach the right member tool quickly.</p></div><div className="welcome-status"><strong>{member.status||"active"}</strong><small>Account status</small></div></section>{pct<100?<section className="profile-completion-card"><div><h2>Complete your profile — {pct}%</h2><p>Remaining: {(profile.missingFields||[]).join(", ")||"Finish your profile details."}</p><div className="completion-track"><div className="completion-progress" style={{width:`${pct}%`}}/></div></div><Link className="portal-btn" to="/member/profile">Complete profile</Link></section>:<section className="profile-unlocked-card"><span className="profile-unlocked-kicker">PROFILE COMPLETE</span><h2>Congratulations — your profile is 100% complete.</h2><p>Your member tools are ready.</p></section>}<section className="trust-strip"><div><span>LIVE CONSTITUTION SNAPSHOT</span><h2>What is happening now</h2></div><div className="trust-strip-grid"><Stat v={c.totalMembers} t="Members"/><Stat v={c.totalLeaders} t="Leaders"/><Stat v={c.activeMembers} t="Active"/><Stat v={c.suspendedMembers} t="Suspended"/><Stat v={c.approvedClaims} t="Approved support claims"/><Stat v={money(c.bookBalance)} t="Account book balance"/></div></section><section className="modern-card-grid"><Quick to="/member/profile" t="Profile" x="Update your required member information."/><Quick to="/member/dependents" t="Dependents" x="Add and manage dependants."/><Quick to="/member/accounts" t="Accounts" x="View your ledger and contribution summary."/><Quick to="/member/support" t="Support" x="Submit a support request."/><Quick to="/member/claims" t="Claims" x="Follow what leaders are doing with your case."/><Quick to="/member/messages" t="Chat" x="Message members and leaders."/><Quick to="/member/polls" t="Polls" x="Vote and review previous results."/><Quick to="/member/guide" t="Portal guide" x="Read how to use each portal page."/></section><section className="portal-panel"><h2>Recent announcements</h2>{(d.announcements||[]).slice(0,4).map((n,i)=><div key={n._id||i} className="portal-panel"><strong>{n.title||"Announcement"}</strong><p>{n.excerpt||n.content||n.message||""}</p></div>)}</section></div></DashboardLayout>}function Stat({v,t}){return <div><strong>{v||0}</strong><span>{t}</span></div>}function Quick({to,t,x}){return <Link className="modern-card" to={to}><h3>{t}</h3><p>{x}</p><span>Open →</span></Link>}const money=v=>new Intl.NumberFormat("en-KE",{style:"currency",currency:"KES",maximumFractionDigits:0}).format(Number(v||0));