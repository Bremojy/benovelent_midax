import {useEffect,useState} from "react";import DashboardLayout from "../../layouts/DashboardLayout";import API from "../../services/api";import "../../styles/portalModule.css";export default function Contributions(){const[d,setD]=useState(null),[l,setL]=useState(true),[e,setE]=useState("");const y=new Date().getFullYear();useEffect(()=>{API.get(`/member/accounts?year=${y}`).then(r=>setD(r.data)).catch(x=>setE(x.response?.data?.message||x.message)).finally(()=>setL(false))},[]);if(l)return <DashboardLayout><div className="portal-empty">Loading Accounts...</div></DashboardLayout>;if(e)return <DashboardLayout><div className="portal-alert">{e}</div></DashboardLayout>;const t=d?.totals||{};return <DashboardLayout><div className="portal-module"><header className="portal-module-header"><div><span>BENEVOLENT ACCOUNTING</span><h1>Accounts</h1><p>Ledger-style view of your scheme activity.</p></div><button className="portal-btn" onClick={()=>window.print()}>Print / Download</button></header><div className="portal-stat-grid"><Stat label="Contributed this year" value={money(t.contributedThisYear)}/><Stat label="Ledger balance" value={money(t.ledgerBalance)}/><Stat label="Cases helped" value={t.totalCasesHelped||0}/><Stat label="Pending claims" value={t.pendingClaims||0}/></div><section className="portal-panel"><h2>Monthly contribution summary — {y}</h2><div className="portal-table-wrap"><table className="portal-table"><thead><tr><th>Month</th><th>Amount contributed</th><th>Contributing records</th></tr></thead><tbody>{(d.monthly||[]).map(m=><tr key={m.month}><td>{new Date(2000,m.month-1,1).toLocaleString("en-KE",{month:"long"})}</td><td>{money(m.contributed)}</td><td>{m.contributingMembers}</td></tr>)}</tbody></table></div></section><section className="portal-panel"><h2>Ledger</h2><div className="portal-table-wrap"><table className="portal-table"><thead><tr><th>Date</th><th>Type</th><th>Description</th><th>Amount</th><th>Status</th></tr></thead><tbody>{(d.transactions||[]).map((x,i)=><tr key={x._id||i}><td>{x.transactionDate?new Date(x.transactionDate).toLocaleDateString("en-KE"):"—"}</td><td>{x.type}</td><td>{x.description||x.category||"—"}</td><td>{money(x.amount)}</td><td><span className="portal-badge">{x.status}</span></td></tr>)}</tbody></table></div></section></div></DashboardLayout>}function Stat({label,value}){return <div className="portal-stat"><span>{label}</span><strong>{value}</strong></div>}const money=v=>new Intl.NumberFormat("en-KE",{style:"currency",currency:"KES",maximumFractionDigits:0}).format(Number(v||0));